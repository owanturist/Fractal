{
  "name": "Maybe",
  "version": "0.0.1",
  "description": "Maybe monad",
  "main": "index.js",
  "repository": {},
  "author": "Anton Ovechkin <owanturist@gmail.com>",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf ./dist",
    "prebuild": "npm run clean",
    "build": "tsc",
    "build:watch": "npm run build -- -w",
    "test": "npm run build -- -p tsconfig.test.json && ava --verbose"
  },
  "devDependencies": {
    "ava": "^0.23.0",
    "typescript": "^2.6.1"
  },
  "dependencies": {
    "@types/lodash": "^4.14.85",
    "lodash": "^4.17.4"
  }
}
